spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:mydb
      username: sa
      password: pass1234
      driverClassName: org.h2.Driver
  flyway:
    enabled: false

pl:
  futurecollars:
    invoicing:
      db: jpa